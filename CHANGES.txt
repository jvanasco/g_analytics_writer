0.2
* initial commit
* forked from gaq_hub
** renamed `google_analytics_writer`
* dropped pylons support
* the package now handles an internal format for tracking the data, and writes
  in one of multiple google analytics formats
